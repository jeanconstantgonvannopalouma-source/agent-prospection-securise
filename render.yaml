services:
  - type: web
    name: agent-prospection-securise
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: ENV
        value: production
